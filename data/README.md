# Data Storage Directory

This folder holds local dataset files for the SIH26056 project:

- `data/static/`: Folder for offline spreadsheets (`.csv`, `.xlsx`) uploaded or provided for batch historical ingestion.
- `data/scraped/`: Folder for raw or staged JSON/HTML data collected during dynamic web scraping runs.
